---
const { images } = Astro.props;
---

<section class="max-w-7xl mx-auto px-4 py-8 space-y-8">
  <div class="w-full md:w-[70%] mx-auto space-y-8">
    {images.map((img, i) => (
      <img src={img} alt={`상세 이미지 ${i + 1}`} class="w-full rounded shadow" />
    ))}
  </div>
</section>
